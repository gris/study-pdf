# `npm version` tags as "v0.4.2" by default, but the release workflow only
# triggers on tags without the prefix ("0.4.2" -- Obsidian's convention, since
# manifest.json's version field has none). A "v"-prefixed tag pushes silently
# and builds nothing.
tag-version-prefix=""
